{% extends 'base.html.twig' %}

{% block body %}
<section class="fil-ariane">
        <div class="container">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="accueil">Accueil</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Festival</li>
                </ol>
            </nav>
        </div>
    </section>
    <main class="festival">
        <section class="plan">
            <div class="container">
                <div class="row">
                    <div class="col-md-5">
                        <h1>Le Festival</h1>
                    </div>
                    <div class="col-md-7">
                    {% for image in plan %}
                        <img src="{{ image.image }}" class="img-fluid" alt="...">
                    {% endfor %}
                    </div>
                </div>
                <div class="ligne"></div>
                <h1 class="programme">programme</h1>
            </div>
        </section>
        <section class="horaires">
            <div class="container">
                <div class="frise">
                    {% for artiste in artistes %}
                        <div class="point">
                            <div class="trait"></div>
                            <div class="artiste">
                                <p>{{ artiste.date_evenement }}</p>
                                <p>{{ artiste.titre }}</p>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>
        <section class="restauration">
            <div class="container">
                <h1>Restauration</h1>
                <div class="row">
                    <div class="col-md-9 contenu">
                    {% for article in restauration %}
                        {{ article.contenu | raw }}
                    {% endfor %}
                    </div>
                </div>
                <div class="ligne"></div>
            </div>
        </section>
        <section class="informations">
            <div class="container">
                <h1>Informations</h1>
                <div class="row">
                    <div class="col-md-9 offset-md-3">
                        <p>Horaires : 18h00 à 1H…</p>
                        <p>Adresse : Vouneuil sous Biard 86580</p>
                        <p>Restauration : plat chaud offert (dans la limite des stocks)</p>
                        <p>Stationnement : Parkings à proximité</p>
                        <p>Afin de préserver la tranquillité des riverains, chaque festivalier s'engage à respecter l'horaire de fermeture, le matériel communal et privé et les personnes qu'il pourrait croiser. Les équipements collectifs, toilettes, lieu de restauration… sont à respecter également.</p>
                        <p>Le site est accessible aux personnes à mobilité réduite. </p>
                    </div>
                </div>
            </div>
        </section>
    </main>
{% endblock %}